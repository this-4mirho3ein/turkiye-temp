export enum Language {
  Fa = "fa", // Farsi
  En = "en", // English
}

export enum HeroUiColors {
  Primary = "primary",
  Secondary = "secondary",
  Success = "success",
  Danger = "danger",
  Warning = "warning",
  Default = "default",
}

export enum SortOrder {
  Oldest = "old",
  Newest = "new",
  Chepeast = "cheapest",
  MostExpensive = "expensive",
}

export enum Premium {
  Ladder = "ladder",
  Special = "special",
  Urgent = "urgent",
}

export enum Roles {
  Agency = "agency",
  Consultor = "consultor",
  User = "user",
}

export enum FilterTypeEnum {
  STRING = "string",
  NUMBER = "number",
  BOOLEAN = "boolean",
  ENUM = "enum",
  RANGE = "range",
}
